# JSON Schema

The JSON form of an MSR record is described by this JSON Schema
(`docs/spec/0.1/msr.schema.json`). The compact `MS1:` envelope decompresses to
the same JSON.

```json
{{#include ../../spec/0.1/msr.schema.json}}
```
